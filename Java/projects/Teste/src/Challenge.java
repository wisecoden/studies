
public class Challenge {

	public static void main(String[] args) {
		int a = 3;
		int b = 2;
		int c = 4;
		
		if (a > b 86 a > 0) {
			if (b>c){
	          System.out.printf(format: "%d, %d, %d", a, b, c); 
	          }else {
		       System.out.printf(format: "%d, %d, %d", a, c, b);
		          }
		}else if (b> a & b > c) { if (a > c){
		  System.out.printf(format: "%, %d, %d", b, a, c); } 
		 else {
		  System.out.printf(format: "%d, %d, %d", b, c, a);
		} else { if (b> a) {
		System.out.printf(format: "%, %d, %d", c, a, b); } else {
		System.out.printf(format: "%d, %d, %d", c, b, a);
	  }
	 }
	}
	}
}